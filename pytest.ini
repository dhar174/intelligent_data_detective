[pytest]
testpaths = tests
markers =
    unit: fast unit tests, no API keys required
    integration: integration tests with mocked LLM, no API keys required
    trajectory: end-to-end trajectory evaluation, requires OPENAI_API_KEY
    slow: slow tests (>5s)
addopts = -v --tb=short
